namespace Workflow.Models
{
    /// <summary>
    /// The type of workflow that this instance is for.
    /// </summary>
    public enum WorkflowType
    {
        Publish = 1,
        Unpublish = 2
    }
}
